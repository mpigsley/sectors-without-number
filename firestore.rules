service cloud.firestore {
  match /databases/{database}/documents {
    match /sectors/{anyDocument} {
      allow create: if request.auth != null;
      allow update: if request.resource.data.creator == request.auth.uid;
      allow read: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}